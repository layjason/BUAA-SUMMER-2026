[project]
name = "qa-seed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
]

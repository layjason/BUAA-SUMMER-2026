# MayoiStar CLIP GPU 服务环境变量模板
# 复制本文件为 .env 并填入实际值

# === 运行模式 ===
# kafka: Kafka Consumer + HTTP health/metrics (生产)
# fastapi: 仅 HTTP API (调试)
MODE=kafka

# === 服务端口 ===
CLIP_PORT=8000

# === Kafka 连接 ===
# GPU 机器通过 VPN/专线连接 Java 服务器的 Kafka 外部端口
KAFKA_BOOTSTRAP_SERVERS=<VPN_IP>:9092
KAFKA_GROUP_ID=mayoistar-clip-gpu
KAFKA_REQUEST_TOPIC=clip-classify-request
KAFKA_RESPONSE_TOPIC=clip-classify-response
# 同一条消息最大重试次数
CONSUMER_MAX_RETRIES=3

# === S3 (RustFS) 连接 ===
# GPU 机器通过 VPN/专线连接 Java 服务器的 RustFS S3 端口
RUSTFS_ENDPOINT=http://<VPN_IP>:9000
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin
RUSTFS_BUCKET=mayoistar-media
